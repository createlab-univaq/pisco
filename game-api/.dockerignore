Dockerfile
.gitignore
README.md
.github/

# Installed by `npm ci` inside the image. Without this, `COPY . .` overwrites
# that with the host's tree -- built for the wrong platform on a Windows/macOS
# dev machine, and needlessly large.
node_modules

# Never bake real credentials into the image
.env
.env.*

docker-compose.yml
API.md
