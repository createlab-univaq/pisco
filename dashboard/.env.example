# Base URL of the analyst API this dashboard talks to.
# NOTE: baked into the compiled web bundle at build time (see Dockerfile), so
# in Coolify this must be set as a BUILD variable, not a runtime one.
# No trailing slash -- paths are appended directly (eg. $API_URL/auth/login).
API_URL=https://api.example.com
