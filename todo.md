nella dashboard 3 tabelle
1. tabella per i singoli nodi (ogni riga è un nodo)
2. tabella per esercitazioni (ogni è un nodo esercitazioni)
3. tabella pre-post (ogni riga è un nodo -> nodo esercitazione -> nodo)
    * punteggio pre punteggio post con un - sia raw number che percentuale
switchare queste tabelle in base alla run e al codice


un singolo grafico a linee che switchi in base al codice del path, ogni linea è un tipo di nodo (no nodi esercitazione)
x sono le run
y punteggio


grafico a barre switchi in base al codice e alla run, ogni barra è un tipo di nodo (no nodi esercitazioni)
y punteggio percentuale che ha fatto l'utente
x il tipo di nodo

le diagnosi sono in generale sul paziente

mettere un isExercise lato polyglot per capire quando un nodo è un nodo esercitazione o meno